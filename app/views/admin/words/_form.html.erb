<%= form_for(@admin_word) do |f| %>
  <% if @admin_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_word.errors.count, "error") %> prohibited this admin_word from being saved:</h2>

      <ul>
      <% @admin_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
