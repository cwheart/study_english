<h1>Listing words</h1>

<table>
  <tbody>
    <% @words.each do |word| %>
      <tr>
        <td><%= word.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @words %>

<br>

<%= link_to 'New Word', new_word_path %>
